# This repo is for testing apis only

To set up a dev env run

### `npm start`

for testing run

### `npm test`

To create a production build run

### `npm run build`

That is..... pretty much it for now.
